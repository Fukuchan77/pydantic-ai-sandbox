# 010-context-engineering — Implementation Tasks

Generated by `/sdd-tasks`. Follow rules/tasks-generation.md and
rules/tasks-parallel-analysis.md.

Conventions:
- `- [ ]` pending, `- [x]` done, `- [ ]*` optional/deferrable test.
- `(P)` = safe to run in parallel (no dependency, disjoint boundary).
- Every task (major and sub) declares `_Boundary:_` and `_Depends:_`.
- Requirement coverage lists numeric IDs only, comma-separated.
- TDD (Red-Green-Refactor) is NON-NEGOTIABLE: write the failing test first,
  confirm red, implement minimum to green, refactor.

---

## 1. 契約昇格（ResearchNote + Finding.notes）

distill した高信号ノートを `patterns_contracts` の単一実体へ昇格し、ハンドオフ
carrier である `Finding` に外部メモリフィールドを追加する。README 正本を同期して
既存ドリフトテストで package 一致を機械検証する。

_Boundary:_ `patterns/contracts/src/patterns_contracts/deep_research.py`, `patterns/contracts/src/patterns_contracts/__init__.py`, `patterns/contracts/tests/unit/test_deep_research_contracts.py`, `patterns/deep-research/README.md`
_Depends:_ none
_Requirements:_ 2.1, 2.2, 2.3, 4.1, 4.2, 4.3

- [x] 1.1 `ResearchNote`(BaseModel, `frozen=True`、フィールド `source` / `locator` / `key_point` / `score`)を `deep_research.py` に定義し、`Finding` に `notes: list[ResearchNote]`（既定 `[]`）を追加、package root から再エクスポートする。`test_deep_research_contracts.py` で field-set・既定値 `[]`・frozen 不変性・root reexport を**先に赤化**してから実装で緑化する。全テストは `block_network` + 決定論フェイクでネットワーク I/O ゼロを維持する。
  _Boundary:_ `patterns/contracts/src/patterns_contracts/deep_research.py`, `patterns/contracts/src/patterns_contracts/__init__.py`, `patterns/contracts/tests/unit/test_deep_research_contracts.py`
  _Depends:_ none
  _Requirements:_ 2.1, 2.2, 4.1, 4.2, 4.3
- [x] 1.2 deep-research README の `## パターン契約` fence へ `ResearchNote` を col-0 `class ResearchNote(BaseModel):` として `SearchResult` 直後に挿入し、`Finding` 定義へ `notes: list[ResearchNote]` 行を追記する。既存 `test_contract_drift.py`（無変更）が「README 正本 == package 実体」を緑化することを確認する。
  _Boundary:_ `patterns/deep-research/README.md`
  _Depends:_ 1.1
  _Requirements:_ 2.3

### Implementation Notes

- 1.1: `Field(default_factory=list)` は pyright strict で `reportUnknownVariableType`
  (`list[Unknown]`) になるため `Field(default=[])` を採用。pydantic v2 は mutable default を
  インスタンス毎にコピーするため共有されない（2 インスタンスで分離を実測確認）。
- 1.1 完了時点で drift test 3 件が意図的に赤（README 未同期）。これは Task 1.2
  （README 正本同期）で緑化する設計上のハンドオフであり回帰ではない。
- 1.2: README 正本へ `ResearchNote`（SearchResult 直後）＋ `Finding.notes` 行を挿入し
  drift test 3 件を緑化。parser は field 名のみ比較するため `notes: list[ResearchNote] = []`
  と注釈すれば足りる（package の `default=[]` と表記も一致）。major task 1 完了。

---

## 2. notes.py の契約 import 移行

`notes.py` の local frozen dataclass `ResearchNote` を契約からの import に置換し、
縮約アルゴリズム（dedup / score cap / truncate / `ValueError` loud-fail）は byte 不変で
保つ。

_Boundary:_ `patterns/deep-research/tests/unit/test_notes.py`, `patterns/deep-research/src/patterns_deep_research/notes.py`
_Depends:_ 1
_Requirements:_ 1.2, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3

- [ ] 2.1 `test_notes.py` に「`notes.ResearchNote` が `patterns_contracts.ResearchNote` の単一実体である」検証（移行前は local dataclass のため赤）と、空入力 `distill_notes([]) == []` ケースを追加する。BaseModel 化に伴う等値・frozen・kwargs 構築の追従も併せて固定する。
  _Boundary:_ `patterns/deep-research/tests/unit/test_notes.py`
  _Depends:_ 1.1
  _Requirements:_ 3.1, 3.2, 3.3, 4.1, 4.2
- [ ] 2.2 `notes.py` の local dataclass `ResearchNote` を削除し `patterns_contracts` から import に置換する。`distill_notes` / `compact_digest` / `render_notebook` / `_key_point` のアルゴリズム（最高 score 優先 dedup、score 降順 + `(source, locator)` tiebreak、`max_notes` cap、可視マーカー truncate、非正引数の `ValueError`）は不変で 2.1 を緑化する。
  _Boundary:_ `patterns/deep-research/src/patterns_deep_research/notes.py`
  _Depends:_ 2.1
  _Requirements:_ 1.2, 3.1, 3.2, 3.3, 4.3

### Implementation Notes

<!-- Empty at generation. Implementer appends 1-3 bullet learnings after
completing this major task. -->

---

## 3. compaction DI シームの本線配線（researcher + research）

reflect ループの digest 生成を注入可能シーム化し、ループ後に `Finding.notes` を充填、
最上位エントリへ seam を透過して end-to-end opt-in を可能にする。compression ターンは
full `_results_digest` を維持し citation grounding を保全する。

_Boundary:_ `patterns/deep-research/src/patterns_deep_research/researcher.py`, `patterns/deep-research/tests/unit/test_researcher.py`, `patterns/deep-research/src/patterns_deep_research/research.py`, `patterns/deep-research/tests/unit/test_research.py`
_Depends:_ 1, 2
_Requirements:_ 1.1, 1.2, 1.3, 1.4, 2.2, 4.1, 4.2, 4.3

- [ ] 3.1 `test_researcher.py` に (a) `compact_digest` 注入時に reflect プロンプトがノート縮約 digest になる（赤）、(b) 既定（未注入）時は reflect プロンプトが `_results_digest(collected)` から**独立に組み立てた期待文字列**と `==` 完全一致（FunctionModel でプロンプト捕捉、関数同一性に依拠しない byte 互換ロック）、(c) compression ターンは full `_results_digest` 維持、を先に書く。`researcher.py` に `digest_fn: Callable[[Sequence[SearchResult]], str] = _results_digest` シームを **reflect ループ限定**で追加し、`_results_digest` 引数型を `list` → `Sequence` へ拡幅して全アサートを緑化する。
  _Boundary:_ `patterns/deep-research/src/patterns_deep_research/researcher.py`, `patterns/deep-research/tests/unit/test_researcher.py`
  _Depends:_ 2
  _Requirements:_ 1.1, 1.2, 1.3, 1.4, 4.1, 4.2, 4.3
- [ ] 3.2 (P) ループ終了後に `Finding.notes = distill_notes(collected)` を充填し、ハンドオフを「凝縮サマリ + ノートのみ」に固定する（生トランスクリプト非伝播）。`test_researcher.py` で注入時／未注入時いずれも `Finding.notes` が充填されること、空 `collected` で `[]` になることを赤→緑で検証する。
  _Boundary:_ `patterns/deep-research/src/patterns_deep_research/researcher.py`, `patterns/deep-research/tests/unit/test_researcher.py`
  _Depends:_ 3.1
  _Requirements:_ 2.2, 4.1, 4.2, 4.3
- [ ] 3.3 (P) `run_deep_research` に `digest_fn`（既定 `_results_digest`）を追加し `_research` 内の `run_subquestion` 呼び出しへ透過する。`test_research.py` で end-to-end opt-in（`compact_digest` 注入が sub-researcher の reflect digest へ伝播し、既定は現挙動互換）を赤→緑で検証する。
  _Boundary:_ `patterns/deep-research/src/patterns_deep_research/research.py`, `patterns/deep-research/tests/unit/test_research.py`
  _Depends:_ 3.1
  _Requirements:_ 1.1, 1.2, 4.1, 4.3

### Implementation Notes

<!-- Empty at generation. Implementer appends 1-3 bullet learnings after
completing this major task. -->

---

## 4. ドキュメント反映（本線昇格 + 拡張点）

本線配線済みを解説とベストプラクティス検証へ反映し、v1 非対象（トークン上限トリガの
文脈再初期化）と token-budget seam 拡張点を明文化する。

_Boundary:_ `docs/context-engineering.md`, `patterns/deep-research/README.md`, `specs/best-practices-review/verification.md`
_Depends:_ 3
_Requirements:_ 1.4, 3.4, 5.1, 5.2

- [ ] 4.1 `docs/context-engineering.md` を「diff 提示のみ」から「本線配線済 + 拡張点」へ書き換える。`digest_fn` opt-in 注入手順、compression が full digest 維持である点、v1 非対象（上限トリガの文脈再初期化）と既存 token-budget seam への接続を拡張点として明記する。
  _Boundary:_ `docs/context-engineering.md`
  _Depends:_ 3
  _Requirements:_ 1.4, 3.4, 5.1
- [ ] 4.2 (P) deep-research README の該当節へ本線昇格の準拠状況と token-budget seam 拡張点を追記する（1.2 で挿入済みの契約正本ブロックには触れない）。
  _Boundary:_ `patterns/deep-research/README.md`
  _Depends:_ 3
  _Requirements:_ 3.4, 5.1
- [ ] 4.3 (P) `specs/best-practices-review/verification.md` の観点別テーブル第 5 行「コンテキストエンジニアリング」と「主な検証ポイント」節の bullet（→ 改善提案 P2）を実装済（本 spec）へ反映する。
  _Boundary:_ `specs/best-practices-review/verification.md`
  _Depends:_ 3
  _Requirements:_ 5.2

### Implementation Notes

<!-- Empty at generation. Implementer appends 1-3 bullet learnings after
completing this major task. -->
